# Frontend Environment Variables (Optional)
# The app uses proxy configuration in vite.config.js by default

# If you need to change the backend URL, update vite.config.js
# VITE_API_URL=http://localhost:8000
